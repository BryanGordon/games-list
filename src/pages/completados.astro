---
import Layout from "../layouts/Layout.astro"
// import { useFilter } from "../hooks/useFilter"
import type { GamesData } from '../mocks/data'
import GameCard from "../components/GameCard.astro"
import Button from '../components/ui/Button.astro'
import AddIcon from '../icons/AddIcon.astro'

// const { toComplete, filterGames } = useFilter()
// const games = filterGames(toComplete)

const games:GamesData[] = await fetch('http://localhost:3000/platinar').then(res => res.json())
---
<Layout title="Platinar">
  <header class="flex place-content-center mt-10">
		<h1 class="text-zinc-800 font-medium lg:text-5xl">Completar al 100%</h1>
	</header>

  <Button buttonText='Agregar juego' redirect='/'>
    <AddIcon />
  </Button>
  
  <article class="w-[90%] grid md:grid-cols-2 gap-4 mt-16 mx-auto sm:grid-cols-1">
    {
      games.map((game) => <GameCard games={game}/>)
    }
  </article>
  
</Layout>
